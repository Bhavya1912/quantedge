# Environment Configuration Example
# Copy this to .env.local for development

# App Settings
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=https://quantedge.vercel.app,https://quantedge.in

# Redis Connection (Render will inject this)
REDIS_URL=redis://default:password@redis-host:6379

# PostgreSQL Connection (Render will inject this)
DATABASE_URL=postgresql+asyncpg://user:password@postgres-host:5432/quantedge

# JWT Secret (IMPORTANT: Generate a random 32-char string!)
JWT_SECRET=your-256-bit-random-secret-key-change-in-production

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# NSE API
NSE_BASE_URL=https://www.nseindia.com
NSE_OPTION_CHAIN_URL=https://www.nseindia.com/api/option-chain-indices

# Broker APIs (optional)
UPSTOX_API_KEY=your_upstox_key
UPSTOX_SECRET=your_upstox_secret
UPSTOX_REDIRECT_URI=https://quantedge.in/callback
